services:
  - type: web
    name: ai-finance-management-system
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python create_db.py
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
